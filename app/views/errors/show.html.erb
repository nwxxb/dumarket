<section class="hero is-fullheight-with-navbar">
  <div class="hero-body">
    <div class="">
      <p class="title"><%= @http_code %></p>
      <% if @message.present? %>
        <p class="subtitle"><%= @message %></p>
      <% end %>
    </div>
  </div>
</section>
